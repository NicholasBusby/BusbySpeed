- @products.each do |product|
  .large-4.small-6.columns.end
    %img{src: 'http://lorempixel.com/200/200/transport/'}/
    .panel
      %h5 #{product.name}
      %h6.subheader $#{number_with_precision(product.price, :precision => 2)}
